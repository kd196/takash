# Faz 1.2: Firebase Authentication Görevleri

- `[ ]` `pubspec.yaml` dosyasına `google_sign_in` eklenmesi ve `flutter pub get` çalıştırılması
- `[ ]` `lib/features/auth/domain/user_model.dart` oluşturulması
- `[ ]` `lib/features/auth/data/auth_repository.dart` oluşturulması
- `[ ]` `lib/features/auth/presentation/auth_controller.dart` oluşturulması
- `[ ]` `lib/features/auth/presentation/login_screen.dart` ekranının güncellenmesi
- `[ ]` `lib/features/auth/presentation/register_screen.dart` ekranının güncellenmesi
- `[ ]` `lib/core/providers.dart` Firebase provider'larının aktif edilmesi
- `[ ]` `lib/app/router.dart` auth redirect işleminin aktif edilmesi
- `[ ]` `lib/main.dart` Firebase initialization'ın aktif edilmesi
- `[ ]` Derleme kontrolü (`flutter build windows`)
